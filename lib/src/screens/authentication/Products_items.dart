import 'package:flutter/material.dart';
import 'package:flutter/src/material/date.dart';
class Productslistitems extends StatelessWidget {
  const Productslistitems({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Card(
      child: Column(
        children: [
          Container(
            child:Row(
              children: [Text(""),Text("'" )],
            ) ,
          ),
        ],
      ),
    );
  }
}
